//> using scala "3"

// run this code with this command:
//     $ scala-cli CollsArray.sc

"foo bar".split(" ")             // Array(foo, bar)

"foo bar".split(" ").toList      // List(foo, bar)

"foo bar".split(" ").toVector    // Vector(foo, bar)

"foo bar".split(" ").toBuffer    // ArrayBuffer(foo, bar)


