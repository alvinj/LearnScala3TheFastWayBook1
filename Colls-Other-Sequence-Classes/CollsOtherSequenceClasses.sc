//> using scala "3"

// run this code with this command:
//     $ scala-cli CollsOtherSequenceClasses.sc


// this lesson doesn’t have any examples, but i wanted
// to show that you create a List and Vector in the
// same way:
val a = List(1, 2, 3)
val b = Vector(1, 2, 3)


// when you use ArrayBuffer you need to import it first:
import scala.collection.mutable.ArrayBuffer
val c = ArrayBuffer(1, 2, 3)


